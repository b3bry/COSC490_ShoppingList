
<div class="panel panel-default">
  <div class="panel-body">
    <table class="table table-striped table-hover">
      <thead>
      <th><h2>Name</h2></th>
      <th><h2>Delete</h2></th>
      </thead>
      <% @items.each do |item| %>
          <tr>
            <td class="col-md-6"><h1><%=item.name%></h1></td>
            <td class="col-md-2"><%=link_to 'Delete', item, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger btn-xs pull-right'%></td>
          </tr>
      <%end unless @items.blank?%>
    </table>
  </div>
</div>

<%= link_to 'New Item', items_new_path(:list_id => @list_id),:class => 'btn btn-block btn-primary'%>
